# RunPod Serverless TTS Dependencies
# Minimal set for TTS generation only (no web server, no portal dependencies)

# RunPod SDK
runpod>=1.3.0

# TTS Engine (installed from GitHub)
git+https://github.com/resemble-ai/chatterbox.git

# PyTorch (CUDA support) - required before chatterbox
torch>=2.0.0
torchaudio>=2.0.0
