# RunPod Traditional Serverless TTS Dependencies
# Minimal set for TTS generation with queue-based serverless

# RunPod SDK for traditional serverless
runpod>=1.0.0

# TTS Engine (installed from GitHub)
git+https://github.com/resemble-ai/chatterbox.git

# PyTorch (CUDA support) - required before chatterbox
torch>=2.0.0
torchaudio>=2.0.0
