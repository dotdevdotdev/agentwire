# RunPod Load Balancer TTS Dependencies
# Minimal set for TTS generation with FastAPI HTTP server

# FastAPI HTTP server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Required for agentwire package imports
aiohttp>=3.9.0

# TTS Engine (installed from GitHub)
git+https://github.com/resemble-ai/chatterbox.git

# PyTorch (CUDA support) - required before chatterbox
torch>=2.0.0
torchaudio>=2.0.0
